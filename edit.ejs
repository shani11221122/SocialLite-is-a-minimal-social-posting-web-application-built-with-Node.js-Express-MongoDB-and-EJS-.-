<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 w-full h-screen font-sans">


    <div class="w-full flex justify-end items-center p-6 bg-gradient-to-r from-indigo-700 via-purple-700 to-pink-600 shadow-lg">
        <form action="/logout" method="POST">
            <button type="submit" class="flex items-center gap-2 px-5 py-2 bg-white bg-opacity-90 text-indigo-700 font-semibold rounded-full shadow hover:bg-indigo-600 hover:text-white transition duration-200 border border-indigo-200">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a2 2 0 01-2 2H7a2 2 0 01-2-2V7a2 2 0 012-2h4a2 2 0 012 2v1" />
                </svg>
                Logout
            </button>
        </form>
    </div>

    <div class="flex flex-col items-center min-h-[80vh] bg-gradient-to-br from-indigo-700 via-purple-700 to-pink-600 py-10">
        <form action="/update/<%= post._id %>" method="POST" class="bg-white bg-opacity-90 p-10 rounded-2xl shadow-2xl flex flex-col items-center w-full max-w-xl border border-indigo-200">
            <h2 class="text-3xl font-extrabold mb-6 text-indigo-700 drop-shadow">Edit a Post</h2>
            <textarea class="w-full p-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-4 focus:ring-pink-300 resize-none text-gray-800 shadow" placeholder="What's on your mind?" name="content" rows="5"><%= post.content %></textarea>
            <button type="submit" class="mt-6 px-8 py-3 bg-gradient-to-r from-indigo-600 to-pink-500 text-white font-bold rounded-full shadow-lg hover:from-pink-500 hover:to-indigo-600 transition duration-200 transform hover:scale-105">
                Update Post
            </button>
        </form>
    </div>
</body>
</html>
