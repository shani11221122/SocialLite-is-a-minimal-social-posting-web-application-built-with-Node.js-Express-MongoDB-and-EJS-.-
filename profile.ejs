<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 w-full h-screen font-sans">


    <div class="w-full flex justify-end items-center p-6 bg-gradient-to-r from-indigo-700 via-purple-700 to-pink-600 shadow-lg">
        <form action="/logout" method="POST">
            <button type="submit" class="flex items-center gap-2 px-5 py-2 bg-white bg-opacity-90 text-indigo-700 font-semibold rounded-full shadow hover:bg-indigo-600 hover:text-white transition duration-200 border border-indigo-200">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a2 2 0 01-2 2H7a2 2 0 01-2-2V7a2 2 0 012-2h4a2 2 0 012 2v1" />
                </svg>
                Logout
            </button>
        </form>
    </div>

    <div class="flex flex-col items-center min-h-[80vh] bg-gradient-to-br from-indigo-700 via-purple-700 to-pink-600 py-10">
       
        <form action="/post" method="POST" class="bg-white bg-opacity-90 p-10 rounded-2xl shadow-2xl flex flex-col items-center w-full max-w-xl border border-indigo-200">
            <h2 class="text-3xl font-extrabold mb-6 text-indigo-700 drop-shadow">Create a Post</h2>
            <textarea class="w-full p-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-4 focus:ring-pink-300 resize-none text-gray-800 shadow" placeholder="What's on your mind?" name="content" rows="5"></textarea>
            <button type="submit" class="mt-6 px-8 py-3 bg-gradient-to-r from-indigo-600 to-pink-500 text-white font-bold rounded-full shadow-lg hover:from-pink-500 hover:to-indigo-600 transition duration-200 transform hover:scale-105">
                Post
            </button>
        </form>

       
        <div class="posts mt-12 w-full max-w-2xl mx-auto">
            <h3 class="text-xl font-bold text-white mb-6">Your Posts</h3>
            <div class="space-y-6">
                <% if (posts && posts.length > 0) { %>
                    <% user.posts.reverse().forEach(function(post) { %>
                        <div class="bg-white bg-opacity-90 rounded-xl shadow m-5 p-6 flex flex-col gap-2 border border-indigo-200">
                            <div class="flex items-center justify-between">
                                <h4 class="font-semibold text-indigo-700">@<%= user.username %></h4>
                                <span class="text-xs text-gray-500"><%= post.createdAt ? post.createdAt.toLocaleString() : '' %></span>
                            </div>
                            <p class="text-gray-800 break-words"><%= post.content %></p>
                            <div class="flex gap-4 mt-2">
                                
                                <a class="text-pink-600 hover:underline font-medium" href="/posts/<%= post._id %>/like">Like</a>
                                <a class="text-indigo-600 hover:underline font-medium" href="/posts/<%= post._id %>/edit">Edit</a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="text-white text-center">No posts yet.</div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
